void main() {

    int n = 5;
    int[] intArray = new int[n];

    println("Veuillez saisir une serie de " + n + " nombres.");

    for (int i = 0; i < n; i++) {
        intArray[i] = readInteger("Nombre n" + i + 1);
    }

    int k = readInteger("Quel est le facteur K");

    print("Nouvelle série :");

    for (int i = 0; i < intArray.length; i++) {
        intArray[i] *= k;
        print(" " + intArray[i]);
    }

    println("");
}
